spring:
  datasource:
    url: jdbc:h2:mem:testCarServiceDb;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    driver-class-name: org.h2.Driver
    username: admin
    password:

  flyway:
    locations: classpath:db/migration/h2

  sql:
    init:
      data-locations: classpath:/sql/create_and_fill_tables.sql
